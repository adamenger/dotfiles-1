[user]
    name = Mike Steder
    email = steder@gmail.com
    signingkey = 04C002A2

[core]
    excludesfile = ~/.gitignore_global

[color]
    diff = auto
    status = auto
    branch = auto

[rerere]
    enabled = 1

[alias]
    ci = commit -a
    co = checkout
    df = diff
    lg = log
    lp = log -p
    st = status
    tree = log --graph --decorate --pretty=oneline --abbrev-commit
    push-branch-github = push -u origin
    delete-branch-github = push origin --delete
    alias = config --get-regexp 'alias.*'

[github]
    user = steder
	oauth-token = 0a115bb58d5e529ea249423e57c63ba8483e9f2b
